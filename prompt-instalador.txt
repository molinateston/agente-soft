Quero instalar meu Agente Soft (Telegram + Claude) nesta VPS.

ANTES DE COMEÇAR: entre em MODO AUTOMÁTICO (auto-accept) pra não me perguntar
permissão a cada `bash`/`edit` durante a instalação — são dezenas de passos e
travar em cada um mata o setup. Aperte **Shift+Tab** até aparecer no rodapé
"auto-accept edits ON" (ou o modo equivalente do seu Claude Code). Você fica
nesse modo do começo ao fim; quando terminar, eu volto pro modo normal.


ANTES DE QUALQUER COISA: cheque se você está rodando como root.
  if [ "$(id -u)" = "0" ]; then echo "PARE_ROOT"; fi
Se sair PARE_ROOT, NÃO instale nada. Diga pra mim, em português simples, que
está como chefe da máquina (root) e que isso não funciona (o Claude recusa o
modo automático como root, e o bot quebra na primeira mensagem). Peça pra eu
rodar UMA vez:
  sudo bash -c "$(curl -fsSL https://raw.githubusercontent.com/molinateston/agente-soft/main/bootstrap.sh)"
Isso cria um usuário comum chamado 'agente' e abre um novo Claude Code já
como ele. Quando terminar, eu chamo /squad de novo lá dentro. Aborte por
aqui, não siga.

Se NÃO for root, siga normal.

Primeiro baixe o agente e leia o manual (o download vem ANTES de precisar do arquivo).
Não é clone de repositório: é um pacote pronto e versionado.
  V=$(curl -fsSL https://raw.githubusercontent.com/molinateston/agente-soft/main/VERSAO | tr -d '[:space:]')
  mkdir -p ~/agente-soft
  curl -fsSL -o /tmp/agente-soft.tar.gz "https://raw.githubusercontent.com/molinateston/agente-soft/main/pacote/$V/agente-soft.tar.gz"
  tar -xzf /tmp/agente-soft.tar.gz -C ~/agente-soft && rm -f /tmp/agente-soft.tar.gz
Confira que ~/agente-soft/bridge.cjs e ~/agente-soft/SETUP-AGENTE.md existem antes de seguir.
Se o download falhar, PARE e me explique em português simples — não tente clonar repositório.

Depois leia e execute ~/agente-soft/SETUP-AGENTE.md com a ferramenta Read,
seguindo o passo a passo do início ao fim.

Vou te dar conforme você pedir:
- Nome do agente (padrão da casa: LEON — dá sorte kkk; mas pode ser outro, ex: Bia, Sofia)
- Meu nome
- Token do bot do Telegram (eu crio no @BotFather quando você pedir)

Meu id do Telegram NÃO precisa de @userinfobot: na hora certa eu mando uma mensagem
no próprio bot e você captura meu id direto da API (com prova de identidade).

Sobe a ponte, baixa as skills do método, sobe o serviço (auto-restart),
valida, e me confirma quando estiver no ar conversando comigo no Telegram.
Se travar em algum passo, me explica em português simples o que precisa.
